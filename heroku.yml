version: "3.2"

build:
  docker:
    backend:
      build: ./backend
      volumes:
        - ./backend:/app/backend
      ports:
        - "8000:8000"
      stdin_open: true
      tty: true
    web: frontend/Dockerfile
